#include<stdio.h>
void areapari(int r,float *a,float *p);
int main()
{
 int radius;
 float area,parame;
 printf("enter the value of radius");
 scanf("%d",&radius);
 areapari(radius, &area, &parame);
 printf("area=%f",area);
 printf("parameter=%f",parame);
 return 0;
}
void areapari(int r, float*a, float*p)
{
  *a=3.14*r*r;
  *p=2*3.14*r;
  }
 
